@model CobanaEnergy.Project.Models.Accounts.ClawbacksDashboard.ClawbacksDashboardViewModel
@{
    ViewBag.Title = "Clawbacks Dashboard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="clawbacks-dashboard">
    <div class="dashboard-title">
        Clawbacks Dashboard
    </div>
    <div class="filters d-flex flex-wrap gap-4 mb-4 align-items-end">
        <div>
            <label for="supplierFilter" class="form-label fw-semibold">Supplier</label>
            @Html.DropDownListFor(m => m.SupplierId, Model.Suppliers, "All Suppliers", new { @class = "form-select", id = "supplierFilter" })
        </div>
        <div>
            <label for="startDateFilter" class="form-label fw-semibold">Start Date</label>
            <input type="date" id="startDateFilter" class="form-control" />
        </div>
        <div>
            <label for="endDateFilter" class="form-label fw-semibold">End Date</label>
            <input type="date" id="endDateFilter" class="form-control" />
        </div>
        <div class="ms-auto">
            <button type="button" class="btn btn-success" id="exportExcelBtn">
                <i class="fas fa-file-excel me-2"></i> Export to Excel
            </button>
        </div>
    </div>

    <div class="table-container">
        <table id="clawbacksTable" class="table table-bordered table-striped w-100">
            <thead class="table-light">
                <tr>
                    <th>Business Name</th>
                    <th>MPAN</th>
                    <th>MPRN</th>
                    <th>InputEAC</th>
                    <th>Start Date</th>
                    <th>CED</th>
                    <th>CED(COT)</th>
                    <th>Payment Status</th>
                    <th>Cobana Final Reconciliation</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>

@section styles {
    <link href="~/Content/Accounts/ClawbacksDashboard/ClawbacksDashboard.css" rel="stylesheet" />
}
@section scripts {
    <script src="~/Scripts/Accounts/ClawbacksDashboard/clawbacksDashboard.js"></script>
}
